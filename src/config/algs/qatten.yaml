# --- Qatten specific parameters ---

# use epsilon greedy action selector
action_selector: "epsilon_greedy"
epsilon_start: 1.0
epsilon_finish: 0.05
epsilon_anneal_time: 200000

runner: "episode"

buffer_size: 5000

# update the target network every {} episodes
target_update_interval: 200

# use the Q_Learner to train
agent_output_type: "q"
learner: "qatten_learner"
double_q: True
mixer: "qatten"
mixing_embed_dim: 32
nonlinear: True        # 原为 False。开启非线性混合，增强拟合能力
hypernet_layers: 2     # 增加超网络层数（默认为1）
hypernet_embed: 64
adv_hypernet_layers: 1
adv_hypernet_embed: 64

num_kernel: 4
is_minus_one: True
is_adv_attention: True
is_stop_gradient: True

n_head: 4  # attention head number
attend_reg_coef: 0.001  # attention regulation coefficient  # For MMM2 and 3s5z_vs_3s6z, it is 0.001
state_bias: True  # the constant value c(s) in the paper
mask_dead: False
weighted_head: False  # weighted head Q-values, for MMM2 and 3s5z_vs_3s6z, it is True
nonlinear: False  # non-linearity, for MMM2, it is True

burn_in_period: 100

# 开启 N-step 返回，让奖励更快向前传播
n_step: 5  # 或者 3。这意味着模型会看未来 3-5 步的累计奖励，而不仅仅是下一步。
gamma: 0.99 # 确保折扣因子足够高，重视远期目标

name: "qatten"